import { TableShortPipe } from './table-short.pipe';

describe('TableShortPipe', () => {
  it('create an instance', () => {
    const pipe = new TableShortPipe();
    expect(pipe).toBeTruthy();
  });
});
